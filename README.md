# vis

[`vis`](https://github.com/martanne/vis), packaged for the Zig build system.

```sh
$ zig version
0.16.0
$ zig build
$ ./zig-out/bin/vis
```


> [!NOTE]
> In early stages of development. Come back later for documentation.
